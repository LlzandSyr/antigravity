# C 语言基础知识回归 (面向嵌入式与STM32开发)

在嵌入式开发（如 STM32）中，C 语言不仅是编写逻辑的工具，更是与底层硬件（寄存器、内存）直接交互的桥梁。理解 C 语言的底层机理，能够帮助我们写出更高效、更稳定、更易于调试的代码。

本篇指南将从**指针与内存**、**嵌入式核心关键字**、**位操作**、**结构体与共用体**以及**宏与预处理**五个维度，系统地梳理 C 语言的核心基础知识。

---

## 1. 指针与内存管理 (Pointers & Memory)

指针是 C 语言的灵魂。在嵌入式中，指针本质上就是**内存地址**。

### 1.1 指针的本质与声明
```c
int a = 10;
int *p = &a; // p 保存了变量 a 的内存地址
*p = 20;     // 通过解引用操作符 *，修改 a 的值为 20
```

### 1.2 指针的运算
指针的加减法并不是简单的数值加减，而是以**指针指向的数据类型大小**为单位移动。
```c
uint32_t arr[4] = {10, 20, 30, 40};
uint32_t *p = arr; // 指向 arr[0]

p++; // 此时 p 指向 arr[1]，地址实际上增加了 4 个字节（因为 sizeof(uint32_t) = 4）
```

### 1.3 函数指针 (Function Pointer)
函数指针保存的是函数代码段的入口地址。在嵌入式开发中，广泛应用于**中断服务程序(ISR)映射**、**回调函数(Callback)**以及**事件驱动架构**。
```c
// 声明一个函数指针类型：无返回值，无参数
typedef void (*Callback_t)(void);

void Led_On(void) {
    // 点亮 LED
}

int main(void) {
    Callback_t eventHandler = Led_On; // 赋值
    eventHandler();                   // 调用函数
}
```

---

## 2. 嵌入式 C 语言核心关键字

### 2.1 `volatile`（易变的）
> [!IMPORTANT]
> `volatile` 是嵌入式开发中**最重要**的关键字之一。它告诉编译器：**不要对该变量相关的代码进行优化**，每次读写都必须直接访问内存/寄存器地址，而不是读取 CPU 寄存器或缓存中的备份。

#### 常见应用场景：
1. **硬件寄存器映射**：硬件寄存器的值随时可能被外部硬件改变。
2. **中断服务函数(ISR)中修改的全局变量**：主循环与中断服务程序共享的变量。
3. **多线程/多任务共享的变量**：如 RTOS 中的共享任务标志。

```c
// STM32 寄存器定义通常会加上 volatile
#define GPIOA_ODR  (*(volatile uint32_t *)0x40020014)

volatile uint8_t g_usart_rx_flag = 0; // 中断中修改的标志位
```

### 2.2 `const`（只读）
`const` 用于声明一个只读变量。在资源受限的微控制器中，合理使用 `const` 可以将数据分配到 **Flash（只读存储器）** 中，而不是占用宝贵的 **SRAM**。

```c
const uint8_t calibration_table[256] = { ... }; // 存储在 Flash 中
```

#### 指针与 `const` 的组合（经典面试题）：
*   `const char *p`（或 `char const *p`）：**指针指向的内容不可变**，指针本身可变。
*   `char * const p`：**指针本身不可变**（地址固定），指向的内容可变。
*   `const char * const p`：**指针本身和指向的内容都不可变**。

### 2.3 `static`（静态）
`static` 的作用取决于它的声明位置：
1.  **修饰局部变量**：变量的生命周期延长至整个程序运行期间（存储在全局数据区），且只在第一次调用时初始化，后续调用保持上一次的值。
2.  **修饰全局变量/函数**：限制其作用域仅在**当前源文件(.c)**内，对其他文件隐藏。这在模块化开发中非常重要，可以避免命名冲突（实现“封装”效果）。

---

## 3. 位操作 (Bitwise Operations)

嵌入式开发需要频繁地配置芯片外设的寄存器，因此位操作是不可或缺的基本功。

| 运算符 | 名称 | 描述 |
| :--- | :--- | :--- |
| `&` | 按位与 | 两位均为 1 时，结果为 1 |
| `\|` | 按位或 | 两位中有一位为 1 时，结果为 1 |
| `^` | 按位异或 | 两位不同为 1，相同为 0（常用于取反） |
| `~` | 按位取反 | 1 变 0，0 变 1 |
| `<<` | 左移 | 将二进制位全部左移若干位，右边补 0 |
| `>>` | 右移 | 将二进制位全部右移若干位，左边补符号位或 0 |

### 3.1 常用寄存器操作模板

#### 1. 将特定位置 1 (Set Bit)
使用**按位或 `|`**：
```c
// 将 GPIOA->ODR 的第 5 位置 1，其余位不受影响
GPIOA->ODR |= (1 << 5);
```

#### 2. 将特定位清 0 (Clear Bit)
使用**按位与 `&`** 和 **按位取反 `~`**：
```c
// 将 GPIOA->ODR 的第 5 位清 0，其余位不受影响
GPIOA->ODR &= ~(1 << 5);
```

#### 3. 将特定位翻转 (Toggle Bit)
使用**按位异或 `^`**：
```c
// 翻转 GPIOA->ODR 的第 5 位（原为 0 变 1，原为 1 变 0）
GPIOA->ODR ^= (1 << 5);
```

#### 4. 判断某一位的状态 (Check Bit)
```c
// 判断 GPIOA->IDR 的第 5 位是否为高电平
if (GPIOA->IDR & (1 << 5)) {
    // 第 5 位为 1 的逻辑
}
```

#### 5. 连续多位的修改 (Clear and Set)
如果需要把某几位修改为特定值，应**先清零、再赋值**：
```c
// 假设需要将寄存器的 [3:2] 位配置为 0b10 (2)
// 1. 先将 [3:2] 位清零 (MASK 为 0b11 << 2)
REG &= ~(0x3 << 2);
// 2. 再将对应值写入
REG |= (0x2 << 2);
```

---

## 4. 结构体、共用体与内存对齐

### 4.1 结构体 (Struct) 与寄存器映射
在 STM32 中，外设寄存器通常是连续分布的。CMSIS 库利用**结构体**的成员地址连续性，来实现外设寄存器的映射：
```c
typedef struct {
    volatile uint32_t MODER;   // 地址偏移 0x00
    volatile uint32_t OTYPER;  // 地址偏移 0x04
    volatile uint32_t OSPEEDR; // 地址偏移 0x08
    volatile uint32_t PUPDR;   // 地址偏移 0x0C
} GPIO_TypeDef;

#define GPIOA ((GPIO_TypeDef *)0x40020000) // 映射到外设基地址
```

### 4.2 内存对齐 (Alignment)
为了提高 CPU 的内存访问效率，编译器通常会对结构体进行**内存对齐**（默认对齐到 2 或 4/8 字节）。
但在**通信协议解析**（如串口、CAN、网络数据包）中，我们希望数据紧凑排列，此时需要取消对齐：
```c
// 使用 GCC 属性取消内存对齐，确保结构体大小等于各成员大小之和
typedef struct __attribute__((packed)) {
    uint8_t  cmd;    // 1 字节
    uint32_t value;  // 4 字节
} Packet_t; // sizeof(Packet_t) = 5 字节，若不对齐则可能被填充为 8 字节
```

### 4.3 共用体 (Union) 的妙用
共用体所有成员共享同一块物理内存。

#### 协议解析与字节序转换：
通过共用体，我们可以轻松地实现浮点数或整型数与字节数组的转换，这在数据发送（如串口发送 float 数据）时极其便利：
```c
typedef union {
    float f_val;
    uint8_t bytes[4];
} FloatPack_t;

FloatPack_t data;
data.f_val = 3.14f;
// 此时可以直接通过 data.bytes[0] ~ data.bytes[3] 逐字节发送该浮点数
```

---

## 5. 宏与预处理 (Macros & Preprocessor)

### 5.1 宏定义的保护括号
在定义带参数的宏时，**务必为每个参数及整个表达式加上括号**，以防止运算符优先级带来的逻辑错误。
```c
// 错误写法
#define SQUARE(x) x * x    // 若计算 SQUARE(1 + 2)，会展开为 1 + 2 * 1 + 2 = 5

// 正确写法
#define SQUARE(x) ((x) * (x)) // 计算 SQUARE(1 + 2) 展开为 ((1 + 2) * (1 + 2)) = 9
```

### 5.2 多语句宏的黄金法则：`do { ... } while(0)`
如果在宏中包含多条语句，为了保证其在 `if-else` 分支中能正常编译和运行，必须使用 `do-while(0)` 包裹：
```c
#define SWAP(a, b) do { \
    int temp = a; \
    a = b; \
    b = temp; \
} while(0)
```

### 5.3 内联函数 (`static inline`) vs 宏
宏是在预处理阶段进行简单的文本替换，没有类型安全检查，且调试困难。
对于性能要求高且代码简短的函数，C99 引入了 `inline` 关键字。**内联函数不仅保留了宏的高效（无函数调用开销），还提供了编译器的类型检查和调试支持**。
```c
static inline int max(int a, int b) {
    return (a > b) ? a : b;
}
```

---

> 指针操纵内存，位操作配置硬件，`volatile` 维护数据一致性，结构体组织寄存器。熟练掌握这四大基石，能让你在嵌入式 C 语言开发中如鱼得水！
