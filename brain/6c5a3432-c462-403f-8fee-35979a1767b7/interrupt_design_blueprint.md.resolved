# STM32 外部中断开发“三环一体”标准设计蓝图

在 STM32（STMicroelectronics 32-bit Microcontroller，意法半导体32位微控制器） 的底层开发中，任何外部中断（EXTI）的编写，都必须遵循一套极度严密的**“信号传导因果链”**。如果脱离了这个因果链去死记硬背代码步骤，极易遗漏关键寄存器的配置（如漏掉时钟、漏掉重映射等）。

本蓝图将为您梳理一套符合物理和逻辑学思维的**“三环一体”代码编写思维思路**：

```mermaid
graph TD
    subgraph 第一环：物理信号输入环
        A[1. 开启时钟电闸: GPIO/SYSCFG] --> B[2. GPIO引脚配置: 输入/上拉]
        B --> C[3. 物理引脚绑定: 选定EXTI线]
    end
    subgraph 第二环：信号拦截与优先级仲裁环
        C --> D[4. EXTI触发配置: 边沿检测/解除屏蔽]
        D --> E[5. NVIC优先级配置: 分组/通道使能]
    end
    subgraph 第三环：业务处理与警报销账环
        E --> F[6. 编写ISR中断服务函数]
        F --> G[7. EXTI_GetITStatus逻辑校验]
        G --> H[8. 翻转LED业务逻辑]
        H --> I[9. EXTI_ClearITPendingBit手动销账]
    end
```

---

## 核心思维：如何通过“因果链”推导编写步骤？

### 第一环：物理信号输入环（引脚到中断线的通路）
*   **思考起点（因）**：按键是物理器件，要将人类手指按下的机械跳变变为电平跳变，首先必须激活物理引脚。
*   **推导步骤**：
    1.  **「电力先行」**：电平采集必须供电。调用 `RCC_AHB1PeriphClockCmd` 开启按键所在的 GPIO 端口时钟。同时，为了配置引脚重映射，调用 `RCC_APB2PeriphClockCmd` 开启 SYSCFG（System Configuration Controller，系统配置控制器） 的时钟。
    2.  **「引脚定义」**：调用 `GPIO_Init`，根据硬件电路（按键按下接地），将引脚配置为**上拉输入**模式，保证常态电平稳定。
    3.  **「通道绑定」**：调用 `SYSCFG_EXTILineConfig`，拨动多路分选器，把引脚（如 `PA0`）绑定到对应的中断线（如 `EXTI_Line0`），彻底打通引脚到 EXTI 之间的电气通路。

---

### 第二环：信号拦截与优先级仲裁环（中断信号到内核的通路）
*   **思考起点（因）**：电气信号已经来到了 EXTI 控制器门口，我们需要规定什么样的电平变化才算“警报”，并且当多个警报同时拉响时，内核应该先理谁。
*   **推导步骤**：
    4.  **「拦截设置」**：调用 `EXTI_Init`。设定触发特征（按键按下是高电平变低电平，故选用**下降沿触发**）。设定工作模式为**中断模式**。最重要的动作是**开启中断屏蔽线**（`EXTI_LineCmd = ENABLE`，即允许该信号穿过 EXTI 控制器送往内核）。
    5.  **「安全分组」**：在 NVIC 统一调度之前，必须先设定整体的游戏规则。调用 `NVIC_PriorityGroupConfig` 设置**中断优先级分组**（如 Group 2，明确抢占与子优先级的 bit 位配比）。
    6.  **「排定座次」**：调用 `NVIC_Init`。分别给这几个中断线（如 `EXTI0_IRQn`, `EXTI2_IRQn`）设置不同的抢占优先级，并正式在内核中**使能（开启）**这些 NVIC 物理中断通道。

---

### 第三环：业务处理与警报销账环（软件响应与安全闭环）
*   **思考起点（因）**：当硬件中断信号顺利送达 CPU（Central Processing Unit，中央处理器） 内核，CPU 被强行暂停当前指令，需要跳转到相应的软件函数执行具体业务，并在执行完毕后安全返回。
*   **推导步骤**：
    7.  **「入口对接」**：在 C 代码底端编写函数名必须与启动汇编文件向量表一字不差的**中断服务函数**（如 `void EXTI0_IRQHandler(void)`）。
    8.  **「双重校验」**：在函数内部，第一件事是调用 `EXTI_GetITStatus(EXTI_Line0) != RESET`。这**不是读取引脚上的物理电压**，而是读取 EXTI 控制器内部挂起寄存器（PR（Pending Register，挂起寄存器））的“打卡标志位”，确保这次中断是真实且合法的硬件跳变触发，防止杂波误触发。
    9.  **「业务落地」**：执行您的控制代码（如翻转 LED 状态 `PFout(9) = !PFout(9);`）。
    10. **「警报销账（至关重要！）」**：在退出函数前，必须调用 `EXTI_ClearITPendingBit(EXTI_Line0)`。这一步是通过往 `EXTI_PR` 寄存器对应位写 `1` 来拔掉之前的“打卡红旗”。**如果不清零，CPU 刚出中断又会被立刻再次带入，导致主循环彻底瘫痪！**

---

> [!IMPORTANT]
> **设计方法论总结**
> 任何 STM32 的外设开发（不仅是 EXTI，还包括定时器、串口、I2C 等）都可以套用这套三环思维：
> **1. 先开时钟并配好 GPIO (物理层) ➔ 2. 再配外设参数与 NVIC (协议与控制层) ➔ 3. 最后在主函数使用并编写 ISR 中断服务程序 (应用业务层)。**
> 按照此逻辑编写代码，能实现真正的“零漏写、逻辑闭环”。
