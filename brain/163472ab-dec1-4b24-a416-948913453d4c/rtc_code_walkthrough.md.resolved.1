# 零基础小白保姆级：STM32 RTC 与中断代码逐行大拆解

亲爱的零基础开发者！如果您是第一次看单片机底层的 C 语言代码，千万不要被那些长长的英文单词和奇形怪状的符号吓到。

这篇文档是专门为您量身定制的。我们不用复杂的专业术语，而是用**最直观的生活比喻**，把每一行代码翻译成“人话”，带您彻底看懂！

---

## 💡 零基础预备知识：代码里的“奇特符号”是什么？

在拆解代码前，我们先认识 4 个经常出现的“常客”：

1. **`void`（空）**：
   意思是“这个函数只干活，不需要给出一个结果数字”。比如 `rtc_init(void)`，它就是只负责把时钟初始化，不需要算出一个结果丢给别人。
2. **`&`（取地址符）**：
   **“地址定位符”**。单片机的内存就像一排排房间。如果您要把一张表交给函数处理，您不能把整个“房子”搬过去，您只需要用 `&` 告诉函数：“我的表放在几号房间，你自己去拿。”
3. **`0x` 前缀（十六进制）**：
   告诉单片机：“后面的数字是十六进制的。” 例如，我们写 `0x14`，它在屏幕上看起来就是 `14`，单片机读到它就会直接当成 `14` 来处理。
4. **`Structure`（结构体 / 填空表）**：
   它就像是**一张多行的纸质申请表**。比如“日期表”，里面有“年、月、日、星期”几个空格。我们先在代码里把空格填满，最后统一交上去。

---

## 🛠️ 第一部分：rtc_init() 初始化函数大拆解

这个函数是**“时钟启动大会”**，负责把整个时钟的齿轮、发条和警报线全部安装好。

### 1. 拿钥匙，开大门 (解密 PWR 备份域)
```c
// 1. 开启电源控制器的“电闸”
RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR, ENABLE);

// 2. 解开避难所大门的挂锁
PWR_BackupAccessCmd(ENABLE);
```
* **人话翻译**：
  单片机为了省电和安全，平时把 RTC 关在一个“绝密保险库（备份域）”里。
  * 第一步，我们先把主电闸 `PWR` 打开（拉电闸通电）。
  * 第二步，使用 `PWR_BackupAccessCmd(ENABLE)` 解开保险库大门的挂锁，允许 CPU 进去修改里面的时间。

---

### 2. 启动“内部电子节拍器” (选择并启动 LSI 时钟)
```c
// 1. 开启内部低速时钟（节拍器）
RCC_LSICmd(ENABLE);

// 2. 检查节拍器，直到它开始稳定地滴答滴答走动
while(RCC_GetFlagStatus(RCC_FLAG_LSIRDY) == RESET);

// 3. 把这个节拍器的输出线插到 RTC 时钟源插口上
RCC_RTCCLKConfig(RCC_RTCCLKSource_LSI);

// 4. 合上 RTC 芯片的总电闸
RCC_RTCCLKCmd(ENABLE);

// 5. 等待时钟同步（让飞快的 CPU 和慢速的时钟对齐）
RTC_WaitForSynchro();
```
* **人话翻译**：
  要数时间，必须有一个稳定的“心跳节拍器”。
  * 我们开启了芯片内部的**低速 LSI 节拍器**（约 32000Hz，也就是 1 秒震动 32000 下）。
  * 用 `while` 循环在门口等着，直到它震动得非常均匀（Ready 准备好）。
  * 然后拉一根线，把它的心跳声接入到 RTC 时钟模块中，并合上 RTC 的总开关。

---

### 3. 心跳分频器：怎么把 32000 下心跳变成“刚好 1 秒”？
```c
// 设置硬件分频表格
RTC_InitStructure.RTC_AsynchPrediv = 0x7F;             // 异步分频：写入 127
RTC_InitStructure.RTC_SynchPrediv = 0xF9;              // 同步分频：写入 249
RTC_InitStructure.RTC_HourFormat = RTC_HourFormat_24;  // 选用 24 小时格式
RTC_Init(&RTC_InitStructure);                          // 把这张配置表交上去生效
```
* **人话翻译**：
  LSI 节拍器 1 秒钟震动 **32000 次**，这也太快了！我们要的是 1 秒走一下。
  * 没关系，单片机内部有两个“除法计算器”（分频器）。
  * 第一个除法器：把 32000 除以 `128`（因为从 0 开始数，所以代码里写 `127`）。此时频率变成了：`32000 / 128 = 250`。
  * 第二个除法器：再除以 `250`（代码里写 `249`）。此时频率变成了：`250 / 250 = 1`！
  * **太妙了！经过两级除法，原本一秒震动 32000 次的心跳，刚好变成了 1 秒震动 1 次！**

---

### 4. 拨动时针和分针 (写入出厂默认时间)
```c
// 1. 填好日期申请表：2017 年 11 月 29 日 星期三
RTC_DateStructure.RTC_Year = 0x17;
RTC_DateStructure.RTC_Month = RTC_Month_November;
RTC_DateStructure.RTC_Date = 0x29;
RTC_DateStructure.RTC_WeekDay = RTC_Weekday_Wednesday;
RTC_SetDate(RTC_Format_BCD, &RTC_DateStructure); // 把日期表递交上去

// 2. 填好时间申请表：下午（PM）14 点 56 分 55 秒
RTC_TimeStructure.RTC_H12     = RTC_H12_PM;
RTC_TimeStructure.RTC_Hours   = 0x14;
RTC_TimeStructure.RTC_Minutes = 0x56;
RTC_TimeStructure.RTC_Seconds = 0x55; 
RTC_SetTime(RTC_Format_BCD, &RTC_TimeStructure); // 把时间表递交上去
```
* **人话翻译**：
  我们拿来两张空白申请表：一张是“日期表”，一张是“时间表”，把数字填好。
  * **💡 为什么数字前面都有 `0x`？**
    因为 RTC 硬件规定必须用 **BCD 码（长得像十进制的十六进制数）**。
    所以如果您想设置 `14` 点，就必须写成 `0x14`。如果您只写十进制的 `14`，单片机在底层翻译时就会完全乱套！

---

### 5. 定好每 1 秒响一次的闹钟 (配置 Wakeup 自动唤醒)
```c
// 1. 先关闭闹钟开关，等配置好了再开
RTC_WakeUpCmd(DISABLE);

// 2. 选择我们刚刚算好的 1Hz（1秒一下）作为闹钟节拍
RTC_WakeUpClockConfig(RTC_WakeUpClock_CK_SPRE_16bits);

// 3. 设定警报计数器：只要响 1 次就报警（代码写 1-1 即 0）
RTC_SetWakeUpCounter(1-1);

// 4. 清除以前残留的警报，并允许 RTC 内部向外发送“警报信号”
RTC_ClearITPendingBit(RTC_IT_WUT);
RTC_ITConfig(RTC_IT_WUT, ENABLE);

// 5. 重新合上闹钟开关
RTC_WakeUpCmd(ENABLE);
```
* **人话翻译**：
  我们想让单片机每隔 1 秒触发一次中断（就像是 1 秒震动一次的节拍器）。
  * 我们把警报节拍绑定在前面做好的 **1Hz** 心跳上。
  * 设定计数器为 `0`（硬件计算时是 `0 + 1 = 1`），意思就是：**每经过 1 个心跳（1秒），就把闹钟敲响一次！**

---

### 6. 插好紧急电话线 (配置 EXTI 22 号外部中断线)
```c
EXTI_InitStructure.EXTI_Line = EXTI_Line22;             // 选用第 22 号紧急线（专连 RTC 唤醒）
EXTI_InitStructure.EXTI_Mode = EXTI_Mode_Interrupt;     // 设为中断触发模式
EXTI_InitStructure.EXTI_Trigger = EXTI_Trigger_Rising;  // 只要信号上升（闹钟响了瞬间），就拉响警报
EXTI_InitStructure.EXTI_LineCmd = ENABLE;               // 使能这根线
EXTI_Init(&EXTI_InitStructure);                         // 递交配置表
```
* **人话翻译**：
  还记得我们聊过的**“紧急接线板（EXTI）”**吗？
  * 这一段就是把 **22 号专用紧急信号线** 牢牢插到接线板上。
  * 告诉它：只要有信号从 22 号线传过来，就代表 RTC 闹钟响了，立刻准备通知市长！

---

### 7. 吩咐贴身秘书：必须接听 22 号线电话 (配置 NVIC 中断通道)
```c
NVIC_InitStructure.NVIC_IRQChannel = RTC_WKUP_IRQn;          // 指定监听名为 RTC_WKUP 的紧急通道
NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 0x03; // 设置抢占优先级为 3（数字越小越紧急）
NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0x03;        // 设置响应优先级为 3
NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;              // 允许接听
NVIC_Init(&NVIC_InitStructure);                              // 吩咐下去
```
* **人话翻译**：
  这一段是正式吩咐市长卧室门外的**“贴身秘书（NVIC）”**：
  “如果听到接线板的 22 号线响起名为 `RTC_WKUP_IRQn` 的电话，无论多忙都必须立刻去把正在睡觉的 CPU 叫醒！”

---

## ⚡ 第二部分：RTC_WKUP_IRQHandler() 中断服务程序大拆解

一旦 1 秒时间到，闹钟敲响，CPU 会立刻放下手头所有事情，像消防员一样瞬间冲进这个函数中：

```c
void RTC_WKUP_IRQHandler(void)
{
	// 1. 检查：这个警报是不是真的是 RTC 唤醒闹钟发出来的？
	if(RTC_GetITStatus(RTC_IT_WUT) != RESET)
	{
		// 2. 是的话，在电脑屏幕上大喊一声“RTC_WKUP_IRQHandler”！
		printf("RTC_WKUP_IRQHandler\r\n");
        
		// 3. 【极度重要】把闹钟里的“红色报警按钮”给按下去，复位警报
		RTC_ClearITPendingBit(RTC_IT_WUT);
		EXTI_ClearITPendingBit(EXTI_Line22);
	} 
}
```
* **💡 为什么第 3 步“清标志位”是铁律？**
  当中断警报响起时，硬件会自动把“红色警报按钮（标志位）”按下去。
  **如果我们在代码最后不手动把它“拔起来复位”**，那么当 CPU 离开这个函数后，它会以为警报还没解除，从而**无限次地重新冲进这个函数**，导致您的单片机直接卡死在这里，什么别的活都干不了！所以必须清标志位。

---

## ⏱️ 第三部分：在 main 主循环里读取并打印时间

在主循环中，我们让单片机每一秒钟，都去向“地下守时人”询问一次当下的时间并打印出来：

```c
// 1. 【先读时间】获取时间寄存器的值（存入 RTC_TimeStructure 房里）
RTC_GetTime(RTC_Format_BCD, &RTC_TimeStructure);
printf("Time %02x:%02x:%02x\r\n",
	RTC_TimeStructure.RTC_Hours,
	RTC_TimeStructure.RTC_Minutes,
	RTC_TimeStructure.RTC_Seconds);

// 2. 【后读日期】获取日期寄存器的值（存入 RTC_DateStructure 房里）
RTC_GetDate(RTC_Format_BCD, &RTC_DateStructure);
printf("Date 20%02x/%02x/%02x Week:%x\r\n",
	RTC_DateStructure.RTC_Year,
	RTC_DateStructure.RTC_Month,
	RTC_DateStructure.RTC_Date,
	RTC_DateStructure.RTC_WeekDay);
```
* **💡 为什么必须“先时间，后日期”？**
  这是 STM32 官方芯片的死规定。为了防止读数据时刚好跨天（比如刚好在 23:59:59 和 00:00:00 之间），当您读 **Time** 时，芯片会自动锁死 **Date** 不让它变；当您读完 **Date** 后，锁才会解开。如果不按这个顺序，读出来的时间和日期就会对不上！
* **💡 为什么用 `%02x` 打印？**
  因为读取出来的是 BCD 十六进制码，比如 `0x14` 代表 14 点。在 C 语言中，`%02x` 是把十六进制数按照“2位宽度、不足补0”的格式打印成十进制模样。所以 `0x14` 打印出来就是漂亮的 `14`！

---

## 🔘 第四部分：按下按键 KEY0 强制改写时间测试

为了让您能够非常开心地进行调试，我们写了下面这一小段代码：

```c
// 1. 检查：板子上的 KEY0 键（PE4引脚）被按下了吗？(0 代表被按下了)
if(GPIO_ReadInputDataBit(GPIOE, GPIO_Pin_4) == 0)
{
	delay_ms(10); // 2. 软件消抖：等 10 毫秒，避开按键刚接触时噼里啪啦的弹跳杂波
	if(GPIO_ReadInputDataBit(GPIOE, GPIO_Pin_4) == 0) // 再次确认：是真的按下了吗？
	{
		rtc_modify_test(); // 3. 强制把时间改写为：23:59:50
		printf("[DEBUG] RTC time modified to 23:59:50 for rollover test!\r\n");
        
		while(GPIO_ReadInputDataBit(GPIOE, GPIO_Pin_4) == 0); // 4. 松手检测：只要您手没抬起来，就卡在这里，防止按一次被疯狂连续触发！
	}
}
```
* **人话翻译**：
  这是一套**工业级、极度规范**的按键检测写法。不仅排除了物理杂波干扰（消抖），还实现了“手不抬起，就不重复触发”的完美逻辑，非常值得零基础开发者好好揣摩和吸收！
