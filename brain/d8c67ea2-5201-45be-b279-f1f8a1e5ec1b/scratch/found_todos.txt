Line 34: 实现思路：弹窗实际尺寸在代码中为 lv_obj_set_size(win, 600, 360)（第840行）。弹窗设置圆角10、阴影宽度50(opacity=50)、边框2px浅灰。弹窗内容区域支持滚动。注：核对表写700x420，源码为600x360，可能后续版本调整。
Line 100: 4-6 开启系统 malloc 并优化主循环提升稳定性，修复 dir_file_list.c 编译错误
Line 101: 实现思路：全局上下文 DBI 结构体通过 malloc(sizeof(DBI)) 分配在 Show_Dirfile_Windows() 中。每个链表节点通过 Create_List_Node() 调用 malloc+memset 分配。主循环 while(!EXIT_MASK) 中 lv_timer_handler() + usleep(10000) 每10ms刷新LVGL。退出后额外执行100次 lv_timer_handler() 清理动画残留。修复编译错误涉及头文件依赖和类型定义。
Line 125: 实现思路：弹窗滑入动画中 X 坐标从 -600 滑入到 100，Y 坐标固定在 60。通过调整动画起始值和目标值来适配弹窗实际尺寸(600x360)，确保弹窗始终居中显示。
Line 183: 3-3 启用路径标签的重绘色功能(recolor)，格式化文本使 'PATH:' 为白色、实际路径为亮绿色
Line 184: 实现思路：在 Dir_Search_Show() 中调用 lv_label_set_recolor(path_label, true) 启用recolor功能。使用格式化字符串 "#FFFFFF PATH:# #00FF00 /path#" 设置路径标签文本，其中 "PATH:" 显示为白色(#FFFFFF)、实际路径显示为亮绿色(#00FF00)。每次切换目录时更新此格式化文本。
Line 211: 实现思路：dir_btn_irq_inf 结构体包含 next 和 prev 前后驱指针，真实图片节点通过 Add_List_Node() 头插法形成一个独立闭环（prev 绕开虚拟头节点 head）。Page_Btn_Event() 向下翻沿 next 遍历，向上翻沿 prev 遍历。沿 prev 遍历时必须跳过虚拟头节点 head——通过哨兵判断 if (tmp == tmp->p_dbi->head) { tmp = tmp->prev; continue; } 防止段错误。循环条件 tmp != pvi->current_node 确保绕一圈停止。跳过非图片节点(只认PNG_TYPE/GIF_TYPE)。只有一张图时 target_node == current_node 判定避免原地重复动画。
Line 256: 2-6 修复核心Bug：修复切换目录时双向链表销毁导致的 double free/corruption 错误（通过断开循环环使其退化为单向链表安全销毁）
Line 411: 实现思路：不是调库，从 pthread_create 到条件变量调度全部手写。核心数据结构 threadpool_t 包含 pthread_mutex_t lock(互斥锁)、pthread_cond_t cond(条件变量)、pthread_t *threads(5个线程ID)、task_t *task_list(FIFO单向任务链表)、int thread_count(5)、int waiting_tasks(等待任务计数)、bool shutdown(销毁标志)。threadpool_init()：malloc 分配 pool → 初始化锁和条件变量 → 分配线程ID数组 → 批量 pthread_create 创建5个工作线程。thread_routine()：工作线程的无限循环体。加锁 → while(task_list==NULL && !shutdown) 调 pthread_cond_wait 阻塞等待 → 被唤醒后检查 shutdown && task_list==NULL 则退出 → 从链表头取任务(FIFO) → waiting_tasks-- → 解锁 → 执行 task->function(task->arg) → free(task) → 回到循环开头继续等待。threadpool_add_task()：创建 task 节点 → 加锁 → 尾插法入队(FIFO公平性) → waiting_tasks++ → pthread_cond_signal 唤醒一个工作线程 → 解锁。threadpool_destroy()：加锁 → shutdown=true → pthread_cond_broadcast 广播唤醒所有线程 → 解锁 → pthread_join 等待所有线程退出 → 遍历清理残留任务链表(防止内存泄漏) → free(threads) → pthread_mutex_destroy → pthread_cond_destroy → free(pool)。主线程 main() 中 pool = threadpool_init(5) 创建线程池，server_run() 中 accept 后 threadpool_add_task(pool, client_service, p_fd) 投递任务。
Line 445: 实现思路：集成了百文网 lv_100ask_pinyin_ime 拼音输入法库，实现开发板上用拼音输入中文用户名和密码。这不是简单的"拿来就用"——修改了库源码：原输入法默认挂在传入的父容器上，页面切换时 lv_obj_del 父容器会级联销毁输入法导致白屏或段错误。修改构造函数将键盘和候选词面板全部挂到 lv_layer_top() 独立顶层图层，与业务页面完全解耦。手动管理 pinyin_ime_obj、kb键盘、cand_panel候选词面板三个对象的显隐和指针生命周期。ta_event_cb() 处理输入法事件：点击输入框弹出拼音键盘(800x240, 对齐底部)，候选词面板置顶。kb_event_cb() 处理 READY/CANCEL 事件隐藏键盘。bg_click_event_cb() 点击背景收起键盘。页面跳转前先隐藏键盘，跳转后安全重置指针为NULL。
Line 560: 实现思路：ServerWindow::initNetwork() 函数中创建 QTcpServer 对象。调用 tcpServer->listen(QHostAddress::Any, AURA_PORT) 监听 9999 端口。连接 newConnection 信号到 onNewConnection() 槽函数。onNewConnection() 中 while(tcpServer->hasPendingConnections()) 遍历待处理连接，调用 nextPendingConnection() 获取 QTcpSocket 指针。为每个客户端在 QHash<QTcpSocket*, QByteArray> clientBuffers 中创建独立接收缓冲区，connect readyRead 和 disconnected 信号到对应槽函数。
Line 859: ⑨ 从全量菜单中匹配 AI 返回的菜品 ID → 新增逻辑（约10行），遍历 AI 返回的 [{id,reason}]，在全量菜单 QJsonArray 中按 id 匹配实际菜品数据
Line 875: d. 遍历 AI 返回的推荐菜品 ID，从全量菜单中匹配实际菜品数据（名称、价格、图片路径）
Line 936: 八、错误处理与降级策略
Line 942: 在 Aura 项目中，我在服务端新增了 AI 智能推荐功能。用户在欢迎页输入"3个人，想吃辣的"，服务端将全量菜单和用户偏好一起发给大模型 API，AI 返回推荐菜品 ID 后，服务端从 SQLite 数据库中匹配实际菜品数据返回给客户端。HTTP 请求用的是 Qt 的 QNetworkAccessManager——跟项目中已有的天气模块完全一样的异步请求模式，通过信号槽机制实现非阻塞回调，不阻塞 TCP 服务器的主事件循环。新增代码约 120 行。AI 调用失败时自动降级为发送普通菜单，用户无感知。这个设计体现了在嵌入式 Qt 应用中集成 AI 能力的工程化思维——不是简单地"调个 API"，而是考虑了降级策略、异步非阻塞、以及与现有 MVC 架构的无缝衔接。