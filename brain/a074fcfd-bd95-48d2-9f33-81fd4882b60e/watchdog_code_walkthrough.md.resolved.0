# 🐕 STM32 独立看门狗代码深度 Walkthrough 讲解

> **作者：安防卫士对账组（Antigravity）**
> **最后更新：2026-05-26**

---

## 🛠️ 第一部分：独立看门狗初始化配置讲解

在 STM32（STM32 32-bit Microcontrollers，意法半导体32位微控制器） 中，IWDG（Independent Watchdog，独立看门狗） 依靠芯片内部完全独立的 LSI（Low Speed Internal，内部低速时钟） 运行，其初始化需要解除写保护、设置分频系数、写入重载值以及启动看门狗。

下面是对配置代码的逐行剖析：

```c
/* 
 * 步骤 1：解除写保护
 * 独立看门狗（IWDG）的预分频寄存器（IWDG_PR）和重载寄存器（IWDG_RLR）是受到写保护 of 的。
 * 为了防止主程序误写这些寄存器导致看门狗失效，在写入前必须先向“键寄存器（IWDG_KR）”写入特定的解锁代码 0x5555。
 * 库函数 IWDG_WriteAccessCmd(IWDG_WriteAccess_Enable) 内部就是执行了这个解锁动作。
 */
IWDG_WriteAccessCmd(IWDG_WriteAccess_Enable);

/* 
 * 步骤 2：设置看门狗的预分频系数
 * 独立看门狗（IWDG）的时钟源来自 LSI（Low Speed Internal，内部低速时钟），标称频率约为 32KHz。
 * 通过设置预分频器（Prescaler），可以将时钟变慢，以便实现更长的倒计时。
 * 这里传入参数 IWDG_Prescaler_256，表示将 LSI（Low Speed Internal，内部低速时钟） 频率进行 256 分频。
 * 此时，看门狗的工作频率 = 32000Hz / 256 = 125Hz (即每秒递减 125 次)。
 */
IWDG_SetPrescaler(IWDG_Prescaler_256);

/* 
 * 步骤 3：设置看门狗的超时计数值（重载值）
 * 看门狗的递减计数器是 12 位的（最大值为 4095）。
 * 当前看门狗的工作频率为 125Hz，这意味着每过 1/125 秒，计数器就会减 1。
 * 如果我们希望看门狗在 1 秒（即 1000 毫秒）不喂狗时自动复位：
 * 重载值 = 1 秒 * 125Hz - 1 = 124 (库函数中传入 125-1)。
 * 如果我们希望看门狗在 2 秒（即 2000 毫秒）不喂狗时自动复位：
 * 重载值 = 2 秒 * 125Hz - 1 = 249 (库函数中传入 250-1)。
 */  
IWDG_SetReload(125-1); // 这里配置为 1 秒超时溢出

/* 
 * 步骤 4：手动将重载值输送到递减计数器（喂狗）
 * 刚刚写入的重载值目前还在重载寄存器（IWDG_RLR）中，并未真正写入当前的递减计数器中。
 * 我们必须向键寄存器（IWDG_KR）写入代码 0xAAAA，通知看门狗硬件将重载寄存器的值物理复制到递减计数器中。
 * 独立看门狗（IWDG）不会像普通的定时器 TIM（Timer，定时器） 那样开机自动复制重载值，必须手动调用一次。
 */
IWDG_ReloadCounter();

/* 
 * 步骤 5：启动独立看门狗
 * 向键寄存器（IWDG_KR）写入代码 0xCCCC，物理使使能并启动独立看门狗（IWDG）。
 * 一旦启动，LSI（Low Speed Internal，内部低速时钟） 会被硬件强制开启，且看门狗计数器开始做冷酷的递减倒计时。
 * 注意：独立看门狗（IWDG）一旦启动，在本次上电运行期间，没有任何软件指令能够将其关闭！只能靠按时喂狗阻止复位。
 */
IWDG_Enable();
```

---

## 📅 第二部分：三种基础喂狗场景的代码对比

看门狗启动后，整个系统的生命线就维系在 `IWDG_ReloadCounter()`（喂狗）这条指令上了。以下是温工课件中给出的三种逻辑场景在 `main` 函数死循环里的表现：

### 场景一：正常按时喂狗（系统高枕无忧）

```c
int main(void)
{
    // ... 初始化硬件、串口、LED（Light Emitting Diode，发光二极管） ...
    
    // 初始化看门狗，超时时间设置为 1 秒
    iwdg_init_1s(); 
    
    while(1)
    {
        /* 
         * 正常喂狗：重置计数器为 124，看门狗重新开始倒计时。
         * 由于我们喂狗后延时了 500 毫秒，在 500 毫秒内计数器只递减了约 62 次，远远没有到达 0。
         * 所以计数器在降为 0 之前又被刷新为了 124，CPU（Central Processing Unit，中央处理器） 绝不会发生复位重启！
         */
        IWDG_ReloadCounter();  
        delay_ms(500); // 快乐地等待 500 毫秒
    }
}
```

### 场景二：超时喂狗（看门狗“冷酷执法”物理复位）

```c
int main(void)
{
    // ... 初始化硬件 ...
    
    // 初始化看门狗，超时时间设置为 1 秒
    iwdg_init_1s(); 
    
    while(1)
    {
        /* 
         * 超时喂狗：
         * 第一次喂狗后，程序开始执行延时。
         * 可是延时总共长达 500 + 600 = 1100 毫秒（即 1.1 秒）。
         * 而我们的看门狗只能撑 1 秒（1000 毫秒）！
         * 在延时进行到第 1000 毫秒的一瞬间，计数器递减到 0，硬件电路立刻拉低复位引脚。
         * CPU（Central Processing Unit，中央处理器） 瞬间复位，程序从头开始执行，后面的代码根本来不及跑。
         */
        IWDG_ReloadCounter();  
        delay_ms(500);
        delay_ms(600); // 致命延时，导致直接漏狗
    }
}
```

### 场景三：程序死循环不喂狗（看门狗自动救市）

```c
int main(void)
{
    // ... 初始化硬件 ...
    
    // 初始化看门狗，超时时间设置为 1 秒
    iwdg_init_1s(); 
    
    while(1)
    {
        /* 
         * 完全不喂狗：
         * 程序进入了业务逻辑的死循环，但由于没有任何喂狗动作。
         * 1 秒钟之后，看门狗自动判定系统陷入瘫痪。
         * 物理触发硬件复位，拯救死机卡死的单片机！
         */
    }
}
```

---

## 🚀 第三部分：高级实战——多任务全局计数检测法讲解

> [!IMPORTANT]
> **切记工程大忌**：
> 如果直接在定时器 TIM（Timer，定时器） 的中断服务函数里“只管喂狗”，虽然看似省心，但如果您的主程序 `main()` 中的业务逻辑卡死了，由于中断具有极高的优先级，中断依然会定时唤醒并顺畅地“喂狗”。此时看门狗就变成了摆设，完全失去了监控主循环的意义。

温工课件给出了极为精妙的**“主循环全局计数检测法”**。下面是其运行机制的详细 walkthrough 讲解：

### 1. 物理运行架构图

```mermaid
graph TD
    A["主循环 main()"] --> B["执行温湿度读取逻辑"]
    B --> C["执行 OLED (Organic Light-Emitting Diode) 显示逻辑"]
    C --> D["执行 RFID (Radio Frequency Identification) 读写逻辑"]
    D --> E["主循环全局计数器递增 g_run_cnt++"]
    E --> A
    
    subgraph 中断系统
        F["定时器中断 (每 100ms 触发一次)"] --> G["累计触发次数 i++"]
        G --> H{"i >= 600 ? (说明过了 60 秒)"}
        H -- "否" --> P["直接跳出中断"]
        H -- "是" --> I{"g_run_cnt == 0 ? (说明这 60 秒内主循环一次都没跑)"}
        I -- "是" --> J["判定异常：故意进入死循环 while(1) 等待看门狗复位"]
        I -- "否" --> K["主循环运行正常：清零 g_run_cnt = 0"]
        K --> L["重置计数器 i = 0"]
        L --> M["屏蔽全局中断，进行安全喂狗 IWDG_ReloadCounter()"]
        M --> P
    end
    
    subgraph 独立看门狗硬件 (LSI 独立时钟)
        N["IWDG 硬件倒计时不断递减"] --> O{"倒计时到 0 ?"}
        O -- "是" --> Q["硬件触发物理复位重启 CPU !"]
        M -. "刷新计数器" .-> N
    end
```

### 2. 代码逐行剖析与运行逻辑

#### ① 主循环端代码（汇报工作）

```c
/* 
 * volatile 关键字：强制编译器每次都从真实的 RAM（Random Access Memory，随机存取存储器） 内存中读取 g_run_cnt。
 * 防止编译器为了“优化”速度，将其缓存在 CPU（Central Processing Unit，中央处理器） 内部的寄存器里，导致中断函数读到脏数据。
 */
static volatile uint32_t g_run_cnt = 0;

int main(void)
{
    // ... 初始化硬件、LED、定时器中断、独立看门狗 ...
    
    while(1)
    {
        // 业务逻辑 1：通过单总线协议读取 DHT11（DHT11 Temperature and Humidity Sensor，温湿度传感器） 温湿度
        read_dht11();
        
        // 业务逻辑 2：通过 SPI（Serial Peripheral Interface，串行外设接口） 驱动 OLED（Organic Light-Emitting Diode，有机发光二极管） 显示器
        oled_show();
        
        // 业务逻辑 3：通过 UART（Universal Asynchronous Receiver Transmitter，通用异步收发器） 读取 RFID（Radio Frequency Identification，射频识别技术） 刷卡器
        rfid_read();
        
        /* 
         * 核心汇报：
         * 只要这三个核心业务逻辑没有卡死，主循环跑完一圈，g_run_cnt 就会加 1。
         * 这相当于向系统证明：“我还活着，我能正常处理业务！”
         */
        g_run_cnt++;
    }
}
```

#### ② 中断监视端代码（严格考评）

```c
/* 
 * 假设我们在 NVIC（Nested Vectored Interrupt Controller，嵌套向量中断控制器） 中配置了定时器中断。
 * 定时器每 100 毫秒（即 0.1 秒）触发一次本中断服务函数。
 */
void TIM3_IRQHandler(void)
{
    static uint32_t i = 0;
    
    // 清除定时器中断挂起位，告诉 CPU 中断已处理，避免反复触发
    if (TIM_GetITStatus(TIM3, TIM_IT_Update) != RESET)
    {
        TIM_ClearITPendingBit(TIM3, TIM_IT_Update);
        
        // 每次进入中断，累计变量加 1
        i++;
        
        /* 
         * 考评心跳窗口：
         * 每 100 毫秒触发一次中断，当 i 累加到 600 次时：
         * 物理流逝时间 = 100ms * 600 = 60000ms = 60 秒（1 分钟）。
         * 这意味着考评员（看门狗监视器）每隔 60 秒，对主程序进行一次全面大体检。
         */
        if(i >= 600)
        {
            /* 
             * 严格考评：
             * 如果这 60 秒期间，g_run_cnt 依然是 0。
             * 这充分证明：主程序可能在 read_dht11() 里因为等待传感器响应死等卡死了，
             * 或者在 rfid_read() 接收中断外死锁了，主循环整整一分钟一圈都没跑过！
             */
            if(g_run_cnt == 0)
            {
                /* 
                 * 绝罚处分：
                 * 既然主程序已经瘫痪，中断绝对不能再喂狗帮其隐瞒灾情。
                 * 中断强行进入 while(1) 无限死循环，罢工拒绝喂狗。
                 * 1 秒钟之后，独立看门狗（IWDG）计数器归零，硬件拉低复位引脚强行重启 CPU（Central Processing Unit，中央处理器）！
                 */
                while(1);
            }
            
            /* 
             * 考核通过：
             * 如果 g_run_cnt 大于 0，说明主程序在过去一分钟运转正常。
             * 考评员重置考核指标：将 g_run_cnt 和累计时间计数 i 全部清零，开始下一轮 60 秒的考核。
             */
            g_run_cnt = 0;
            i = 0;
        }
        
        /* 
         * 执行真正的喂狗操作：
         * 为了防止在写入看门狗时被更高优先级的抢占式中断打断导致写入出错，
         * 我们可以在喂狗的前后进行临界区保护（关总中断与开总中断）。
         */
        __disable_irq();         // 关总中断（进入临界保护区）
        IWDG_ReloadCounter();    // 往键寄存器物理写入 0xAAAA，把沙漏倒转重新加满沙子
        __enable_irq();          // 开总中断（退出临界保护区）
    }
}
```

---

## 📈 第四部分：可靠性指标计算与提升分析

看门狗的加入对单片机系统可用性（Availability）的提升是肉眼可见的。我们可以通过温工给出的可靠性指标来对一下账：

| 可靠性指标 | 英文全称 | 物理定义 | 看门狗加入前 | 看门狗加入后 |
| :--- | :--- | :--- | :--- | :--- |
| **MTTF** | Mean Time To Failure | 发生第一次故障的平均运行时间 | 依赖于环境干扰和软件 Bug 发生概率（不变） | 依赖于环境干扰和软件 Bug 发生概率（不变） |
| **MTTR** | Mean Time To Repair | 发生故障到被修好的平均时间 | **极长**（需人工物理到场，拔电重启，可能耗时几天） | **极短**（看门狗硬件自动检测并在几毫秒内完成重启修复） |
| **系统可用性** | Availability | 系统保持正常运作的概率 | `MTTF / (MTTF + 几天) * 100%` (可用性极低) | `MTTF / (MTTF + 毫秒级) * 100%` (可用性**无限接近 100%**) |

> [!TIP]
> 独立看门狗（IWDG）就像是产品的一道“生命安全线”，使系统即使在极端恶劣环境下，也能处于**“死机 ➔ 毫秒级物理重启自愈 ➔ 重新工作”**的无限健康循环中！
